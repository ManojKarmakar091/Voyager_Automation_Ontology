{
  "name": "mars_demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "npm install &&webdriver-manager update",
    "testSmokeOnDev": "npm run pretest &&protractor config.js --grep=Smoke --NODE_ENV=DEV",
    "testRegressionOnDev": "npm run pretest &&protractor config.js --grep=Regression --NODE_ENV=DEV",
    "testOnDev": "npm run pretest &&protractor config.js --NODE_ENV=DEV",
    "testSmokeOnQA": "npm run pretest &&protractor config.js --grep=Smoke --NODE_ENV=QA",
    "testRegressionOnQA": "npm run pretest &&protractor config.js --grep=Regression --NODE_ENV=QA",
    "testOnQA": "npm run pretest &&protractor config.js --NODE_ENV=QA"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/cosmos": "3.5.2",
    "axios": "0.19.2",
    "azure-keyvault": "^3.0.5",
    "config.ini": "0.0.60",
    "form-data": "^3.0.0",
    "image-to-base64": "^2.0.1",
    "protractor": "^5.4.3",
    "util": "^0.12.2",
    "xlsx": "^0.15.5"
  },
  "devDependencies": {
    "agent-js-jasmine": "^3.0.0",
    "documentdb": "^1.6.0",
    "iniparser": "1.0.5",
    "log4js": "6.1.2"
  }
}
